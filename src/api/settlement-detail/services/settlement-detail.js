'use strict';

/**
 * settlement-detail service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::settlement-detail.settlement-detail');
